export default interface Server
{
	id: number,
	serverName: string,
	serverAddress: string,
	rconPassword: string,
	targetGame: string,
	createdBy: string,
	createdAt: string,
	modifiedBy: string,
	modifiedAt: string
}
